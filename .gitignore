__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
dist2/
.venv/
venv/
env/
data/
*.spec.bak
.vscode/
.idea/
*.log

# Build output and packaged releases
release/

# Internal development notes.
# These are PUBLISHED to the private development repo (Neptune-developement) so a
# collaborator has the reverse-engineering context, and must stay OUT of the public
# release repo. Keep that split in mind before changing this file.
maybach audio/

# Local Linux planning notes: written for us, not upstream. Kept on disk and in the
# handover bundle, but not committed to the port branch or a PR.
/DESIGN.md
/PORT-BEHAVIOR.md
/PORT-FIXES.md
/PORT-PLATFORM.md
/UX-CONTRACT.md
/Neptune.desktop
/linux/FORK-WORKFLOW.md

# Linux port build artifacts
neptune_linux/*.exe
linux/ui-captures/

# Linux port generated reports
linux/live-validation.json
linux/ui-audit.json
